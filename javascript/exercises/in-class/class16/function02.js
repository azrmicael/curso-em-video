function soma(n1=0, n2=0) { 
    return n1 + n2;
}

/*
Como apresentado na linha 1, os valores n1=0 e n2=0 
serão considerados caso o usuário não os forneça na 
chamada (linha 11).
*/

console.log(soma(2, 5));